/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // webpack(config) {
  //   config.module.rules.push({
  //     test: /\.(js|ts)x?$/,
  //     issuer: {
  //       test: /\.(js|ts)x?$/,
  //     },
  //     use: ['@svgr/webpack']
  //   });
  // }
};

module.exports = nextConfig;
